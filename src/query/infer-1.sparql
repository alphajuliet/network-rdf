# Infer that I know all the primary foaf:Person instances

CONSTRUCT {
	?a foaf:knows ?b .
} 
WHERE {
	?a a foaf:Person .
	?a foaf:name "Andrew Joyner" ; 
		gldp:card ?c1 .
	?b a foaf:Person ; 
		gldp:card ?c2 .
}
